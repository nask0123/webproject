<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/forindex.css">
</head>
<body style="background-color: rgb(230, 240, 255);">

    <!-- ✅ Navbar -->
    <nav class="navbar">
        <a href="/index" class="logo">Petition App</a>
        <a href="/user" class="profile-link">My Profile</a>
    </nav>

    <div class="container">
        <h1>Welcome, <%= user.username %>!</h1>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Password:</strong> It is a secret, even the creator doesn't know 😂😂</p>

        <!-- ✅ Show Profile Picture -->
        <% if (user.profileImage) { %>
            <div class="profile-image">
                <img src="data:image/jpeg;base64,<%= user.profileImage %>" alt="Profile Picture"
                     style="max-width: 150px; height: 150px; border-radius: 50%;">
            </div>
        <% } else { %>
            <p>No profile picture uploaded.</p>
        <% } %>

        <!-- ✅ Upload Profile Picture -->
        <form action="/upload-profile" method="POST" enctype="multipart/form-data">
            <input type="file" name="image" accept="image/*" required>
            <button type="submit">Upload Profile Picture</button>
        </form>
    </div>
    <a href="/nike_products">here is the nike</a>
</body>
</html>
